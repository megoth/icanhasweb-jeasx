import Layout from "./Layout";

export default async function () {
  return (
    <Layout
      title={`title`}
      description={`description`}
    >
      <main>
        <h1>ICANHASWEB</h1>
      </main>
    </Layout>
  );
}
